const initialState = {
    boards: ['dsds', 'govno', 'suka']
}

export default function (state = initialState, action){

    switch(action.type){
        default:{
            return state
        }
    }
}